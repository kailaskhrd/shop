<% @roles.each do |role| %>
  <tr>
    <td><%= role.name %></td>
    <td><%= link_to "Edit", edit_role_path(role), remote: true, class: "btn btn-default" %></td>
    <td><%= link_to "Delete", role_delete_path(role), remote: true, class: "btn btn-danger" %></td>
  </tr>
<% end %>